import ContentLayout from '../../components/ContentLayout';
import Heading from '../../components/ContentLayout/Heading';

const Heap = () => {
  return (
    <ContentLayout>
      <Heading title="Heap (Priority Queue)" />
    </ContentLayout>
  );
};

export default Heap;
